name: CI - Plataforma Anime

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout do código
        uses: actions/checkout@v3

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "pnpm"

      - name: 📥 Instalar dependências
        run: pnpm install

      - name: ✅ Rodar testes
        run: pnpm test

      - name: 🔨 Build da aplicação
        run: pnpm run build

# O que esse workflow faz:
#   Aciona quando:
  #   Você faz um push ou pull request na main.
#   Instala dependências com pnpm.
#   Roda os testes com pnpm test.
#   Faz o build da aplicação com pnpm run build.
